<hibernate-configuration>
    <session-factory>
        <!-- SQL dialect
           PostgreSQLDialect does not need to be specified explicitly using 'hibernate.dialect'
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
        -->
        <!-- Connection settings -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/db_dictionary_test</property>
        <property name="hibernate.connection.username">dictionary_user</property>
        <property name="hibernate.connection.password">12345678</property>
        <!-- Print executed SQL to stdout -->
        <property name="show_sql">true</property>
        <!-- Create/update database structure on start -->
        <property name="hibernate.hbm2ddl.auto">validate</property>
    </session-factory>
</hibernate-configuration>